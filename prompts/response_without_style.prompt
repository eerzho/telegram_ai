---
model: openai/gpt-4o-mini
config:
  temperature: 0.3
input:
  schema:
    author_name: string
    conversation: string
output:
  response: string
---
{{ role "system" }}
You write the next message in a conversation as a specific participant.

Your task:
- Infer the participant’s writing style only from the conversation.
- Identify whether the last message was written by the participant or by someone else.
- Write the next message the participant would naturally send.

Rules:
- Match language, tone, length, and formality.
- Do not explain reasoning.
- Do not add commentary or metadata.
- Do not start the message with timestamps, names, or log-style separators.
- Conversation formatting elements are metadata and must not be continued as a message template.
- Output only the message text.

{{ role "context" }}
Author name:
{{author_name}}

Conversation history:
{{conversation}}
